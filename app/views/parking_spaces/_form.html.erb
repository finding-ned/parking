<%= form_with(model: parking_space, local: true) do |form| %>
  <% if parking_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parking_space.errors.count, "error") %> prohibited this parking_space from being saved:</h2>

      <ul>
      <% parking_space.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :space_refno %>
    <%= form.number_field :space_refno %>
  </div>

  <div class="field">
    <%= form.label :space_description %>
    <%= form.text_field :space_description %>
  </div>

  <div class="field">
    <%= form.label :isactive %>
    <%= form.check_box :isactive %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
