<div class="container-fluid">
  <h1>Hello World!</h1>
  <div class="row">
    <div class="col-sm-4" >
      
    </div>
    <div class="col-sm-4" >

      <%= form_with(model: parking_entrance, local: true) do |form| %>
        <% if parking_entrance.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(parking_entrance.errors.count, "error") %> prohibited this parking_entrance from being saved:</h2>

            <ul>
            <% parking_entrance.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= form.label :entrace_refno %>
          <%= form.number_field :entrace_refno %>
        </div>

        <div class="field">
          <%= form.label :entrance_description %>
          <%= form.text_field :entrance_description %>
        </div>

        <div class="field">
          <%= form.label :isactive %>
          <%= form.check_box :isactive %>
        </div>

        <div class="actions">
          <%= form.submit 'Save' %>
        </div>
      <% end %>

      
    </div>


    <div class="col-sm-4" >
      
    </div>

  </div>
</div>
